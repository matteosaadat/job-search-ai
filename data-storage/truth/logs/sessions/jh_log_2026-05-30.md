## Session Log — 2026-05-30

### What was built this session

**Score breakdown population**
- Manually computed and added `score_breakdown` to `job_20260529_2787d654.md` (BETA Technologies, 84.8)
- Dimensions: skill_match=85.8, rate_match=100.0, location_match=85.0 (override), contract_length_match=64.0, timing_match=75.0

**Archived jobs tab**
- Added Active/Archived toggle to Jobs sidebar
- Archived jobs hidden from main list, shown in separate Archived view

**Batch bucket system (full build)**
- [decided] Pursuit scoring model: benefit-of-doubt baseline 70, adjusted by rate/location/contract/signals/company tags
- [decided] Buckets: pursue(≥78), research(≥60), watch(≥42), pass(<42), suspect, signal_play, intelligence
- [decided] Easy Apply is NET POSITIVE with this app (+3) — real role + fast execution advantage
- [decided] Hard apply = +10 (smaller applicant pool — app negates effort cost)
- [decided] Auto-archive only if rate < 50% of minimum (not at minimum itself)
- Built `_compute_pursuit()` in server.py
- Built `jh_triage_job_list` rewrite in mcp.py using bucket model
- Built batch detail view UI with filter tabs (All/Pursue/Research/Watch/Pass/Suspect)
- Built job cards with bucket-colored score rings, signal chips, bucket select dropdown
- Built `PATCH /api/jobs/{job_id}/bucket` and `POST /api/companies/tag` endpoints
- Built `GET /api/jobs/batches/{batch_id}` endpoint
- Fixed `list_batches()` to compute `bucket_counts` dynamically from job files (was reading stale frontmatter)
- Added `→ Digest N Pursue` button in batch view header (handoff from algorithmic → LLM pipeline)

**Pipeline architecture clarified**
- [decided] Two-stage pipeline: Algorithmic (triage/signals/buckets) → LLM (full digest/score/application)
- Handoff is explicit: "→ Digest Pursue" button triggers LLM pipeline for pursue-bucket batch jobs

**Company intelligence**
- `data-storage/companies/tags.json` — tags: preferred(+20), suspect(-35), resume_farmer(-35), slow_process(-5), blacklisted(-50)
- Company tag menu in batch job cards
- [decided] DataAnnotation flagged as suspect — known resume farming / AI gig platform, not real engineering roles

**Chat batch detection**
- UI chat textarea detects multi-job pastes via salary/rating/Easy Apply heuristics
- Shows `📋 Batch detected — ~N jobs` pill when detected
- Sends `type: 'batch'` to server → Claude auto-routes to `jh_ingest_job_list`
- `jh_chat_listen` docstring updated with routing table (chat/batch/scan_email)

**Gmail integration**
- `jh_scan_email_jobs` MCP tool built — searches Gmail for job alert emails, extracts listings, calls `jh_ingest_job_list` per email
- `📧 Email` button in List Batches sidebar header
- Gmail auth: requires `/mcp` → claude.ai Gmail (not yet authenticated this session)

**Chrome extension rebuild**
- Selection-based capture: grabs `window.getSelection()` as HTML via `Range.cloneContents()` — preserves links
- Preview panel: shows first 12 lines of selected text + extracted job links before sending
- "No selection" warning if nothing selected — no more full-page noise
- Sends `{ text, html, source_url, job_type, links }` to server
- Server stores HTML in raw file body alongside plain text
- Auto-queue to Claude after intake: server writes to Claude's inbox if session active and not busy
- Extension shows "Claude is processing…" vs "Open Job Hunt to continue" based on `auto_queued` response

### Known gaps / next steps
- Gmail auth still needed (run `/mcp` → claude.ai Gmail)
- Extension needs reload in Chrome after these changes (`chrome://extensions` → refresh)
- Batch view UI not yet visually verified in browser this session
- `jh_triage_job_list` MCP tool needs real-batch test with the new bucket model
- Email scan: test with real LinkedIn job alert email
